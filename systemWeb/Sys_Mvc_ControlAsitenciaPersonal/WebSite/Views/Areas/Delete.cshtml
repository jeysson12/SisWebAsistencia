@model WebSite.Models.AREAS

@{
    ViewBag.Title = "Areas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@Html.Partial("~/Views/Areas/_Partial_NavPills_Areas.cshtml")

<div class="col-md-9">
    <h2>Eliminar Area</h2>

    <h3>¿Estas seguro que desesas elimnar el area?</h3>
    <div>
        
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayName("ID")
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ID_AREA)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.NOMBRE)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NOMBRE)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-actions no-color">
                <input type="submit" value="Eliminar" class="btn btn-success" /> |
                <input type="button" value="Regresar" class="btn btn-danger" onclick="location.href='@Url.Action("Index", "Areas" )'" />
            </div>
        }
    </div>
    </div>
